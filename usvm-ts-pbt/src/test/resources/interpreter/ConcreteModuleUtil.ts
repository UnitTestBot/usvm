export function defaultEquals(left: unknown, right: unknown): boolean {
  return left === right;
}

export const importedMultiply = (left: number, right: number): number => left * right;

export const exportedNumber = 42;
