class SimpleGeneratorExample {
    simpleFunction(x: boolean, y: number): number {
        if (x) {
            if (y > 14) {
                return 1;
            } else {
                return 2;
            }
        } else {
            return 3;
        }
    }
}
